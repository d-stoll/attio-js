/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { webhooksPostV2Webhooks } from "../../funcs/webhooksPostV2Webhooks.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.PostV2WebhooksRequestBody$inboundSchema,
};

export const tool$webhooksPostV2Webhooks: ToolDefinition<typeof args> = {
  name: "webhooks-post-v2-webhooks",
  description: `Create a webhook

Create a webhook and associated subscriptions.

Required scopes: \`webhook:read-write\`.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await webhooksPostV2Webhooks(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
