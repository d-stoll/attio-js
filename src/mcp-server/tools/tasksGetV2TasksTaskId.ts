/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { tasksGetV2TasksTaskId } from "../../funcs/tasksGetV2TasksTaskId.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetV2TasksTaskIdRequest$inboundSchema,
};

export const tool$tasksGetV2TasksTaskId: ToolDefinition<typeof args> = {
  name: "tasks-get-v2-tasks-task-id",
  description: `Get a task

Get a single task by ID.

Required scopes: \`task:read\`, \`object_configuration:read\`, \`record_permission:read\`, \`user_management:read\`.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await tasksGetV2TasksTaskId(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
