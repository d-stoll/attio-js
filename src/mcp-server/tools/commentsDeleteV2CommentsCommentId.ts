/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { commentsDeleteV2CommentsCommentId } from "../../funcs/commentsDeleteV2CommentsCommentId.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.DeleteV2CommentsCommentIdRequest$inboundSchema,
};

export const tool$commentsDeleteV2CommentsCommentId: ToolDefinition<
  typeof args
> = {
  name: "comments-delete-v2-comments-comment-id",
  description: `Delete a comment

Deletes a comment by ID. If deleting a comment at the head of a thread, all messages in the thread are also deleted.

Required scopes: \`comment:read-write\`.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await commentsDeleteV2CommentsCommentId(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
