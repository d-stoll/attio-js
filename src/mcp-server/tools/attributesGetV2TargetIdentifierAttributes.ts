/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { attributesGetV2TargetIdentifierAttributes } from "../../funcs/attributesGetV2TargetIdentifierAttributes.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetV2TargetIdentifierAttributesRequest$inboundSchema,
};

export const tool$attributesGetV2TargetIdentifierAttributes: ToolDefinition<
  typeof args
> = {
  name: "attributes-get-v2-target-identifier-attributes",
  description: `List attributes

Lists all attributes defined on a specific object or list. Attributes are returned in the order that they are sorted by in the UI.

Required scopes: \`object_configuration:read\`.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await attributesGetV2TargetIdentifierAttributes(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
