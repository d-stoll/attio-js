/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { recordsGetV2ObjectsObjectRecordsRecordId } from "../../funcs/recordsGetV2ObjectsObjectRecordsRecordId.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetV2ObjectsObjectRecordsRecordIdRequest$inboundSchema,
};

export const tool$recordsGetV2ObjectsObjectRecordsRecordId: ToolDefinition<
  typeof args
> = {
  name: "records-get-v2-objects-object-records-record-id",
  description: `Get a record

Gets a single person, company or other record by its \`record_id\`.

Required scopes: \`record_permission:read\`, \`object_configuration:read\`.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await recordsGetV2ObjectsObjectRecordsRecordId(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
