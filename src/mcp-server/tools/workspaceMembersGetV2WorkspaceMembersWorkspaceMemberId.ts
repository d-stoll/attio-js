/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { workspaceMembersGetV2WorkspaceMembersWorkspaceMemberId } from "../../funcs/workspaceMembersGetV2WorkspaceMembersWorkspaceMemberId.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request:
    operations.GetV2WorkspaceMembersWorkspaceMemberIdRequest$inboundSchema,
};

export const tool$workspaceMembersGetV2WorkspaceMembersWorkspaceMemberId:
  ToolDefinition<typeof args> = {
    name: "workspace-members-get-v2-workspace-members-workspace-member-id",
    description: `Get a workspace member

Gets a single workspace member by ID.

Required scopes: \`user_management:read\`.`,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] =
        await workspaceMembersGetV2WorkspaceMembersWorkspaceMemberId(
          client,
          args.request,
          { fetchOptions: { signal: ctx.signal } },
        ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      const value = result.value;

      return formatResult(value, apiCall);
    },
  };
