/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { notesPostV2Notes } from "../../funcs/notesPostV2Notes.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.PostV2NotesRequestBody$inboundSchema,
};

export const tool$notesPostV2Notes: ToolDefinition<typeof args> = {
  name: "notes-post-v2-notes",
  description: `Create a note

Creates a new note for a given record.

Required scopes: \`note:read-write\`, \`object_configuration:read\`, \`record_permission:read\`.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await notesPostV2Notes(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
