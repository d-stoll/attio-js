/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { attributesPostV2TargetIdentifierAttributes } from "../../funcs/attributesPostV2TargetIdentifierAttributes.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.PostV2TargetIdentifierAttributesRequest$inboundSchema,
};

export const tool$attributesPostV2TargetIdentifierAttributes: ToolDefinition<
  typeof args
> = {
  name: "attributes-post-v2-target-identifier-attributes",
  description: `Create an attribute

Creates a new attribute on either an object or a list.

To create an attribute on an object, you must also have the \`object_configuration:read-write\` scope.

To create an attribute on a list, you must also have the \`list_configuration:read-write\` scope.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await attributesPostV2TargetIdentifierAttributes(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
