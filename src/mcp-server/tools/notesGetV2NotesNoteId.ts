/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { notesGetV2NotesNoteId } from "../../funcs/notesGetV2NotesNoteId.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetV2NotesNoteIdRequest$inboundSchema,
};

export const tool$notesGetV2NotesNoteId: ToolDefinition<typeof args> = {
  name: "notes-get-v2-notes-note-id",
  description: `Get a note

Get a single note by ID.

Required scopes: \`note:read\`, \`object_configuration:read\`, \`record_permission:read\`.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await notesGetV2NotesNoteId(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
