/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { entriesPostV2ListsListEntriesQuery } from "../../funcs/entriesPostV2ListsListEntriesQuery.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.PostV2ListsListEntriesQueryRequest$inboundSchema,
};

export const tool$entriesPostV2ListsListEntriesQuery: ToolDefinition<
  typeof args
> = {
  name: "entries-post-v2-lists-list-entries-query",
  description: `List entries

Lists entries in a given list, with the option to filter and sort results.

Required scopes: \`list_entry:read\`, \`list_configuration:read\`.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await entriesPostV2ListsListEntriesQuery(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
