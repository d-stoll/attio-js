/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { notesGetV2Notes } from "../../funcs/notesGetV2Notes.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetV2NotesRequest$inboundSchema,
};

export const tool$notesGetV2Notes: ToolDefinition<typeof args> = {
  name: "notes-get-v2-notes",
  description: `List notes

List notes for all records or for a specific record.

Required scopes: \`note:read\`, \`object_configuration:read\`, \`record_permission:read\`.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await notesGetV2Notes(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
