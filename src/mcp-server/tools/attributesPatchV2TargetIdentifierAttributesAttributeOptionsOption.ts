/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { attributesPatchV2TargetIdentifierAttributesAttributeOptionsOption } from "../../funcs/attributesPatchV2TargetIdentifierAttributesAttributeOptionsOption.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request:
    operations
      .PatchV2TargetIdentifierAttributesAttributeOptionsOptionRequest$inboundSchema,
};

export const tool$attributesPatchV2TargetIdentifierAttributesAttributeOptionsOption:
  ToolDefinition<typeof args> = {
    name:
      "attributes-patch-v2-target-identifier-attributes-attribute-options-option",
    description: `Update a select option

Updates a select option on an attribute on either an object or a list.

Required scopes: \`object_configuration:read-write\`.`,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] =
        await attributesPatchV2TargetIdentifierAttributesAttributeOptionsOption(
          client,
          args.request,
          { fetchOptions: { signal: ctx.signal } },
        ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      const value = result.value;

      return formatResult(value, apiCall);
    },
  };
