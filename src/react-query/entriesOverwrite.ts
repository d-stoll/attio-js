/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import {
  MutationKey,
  useMutation,
  UseMutationResult,
} from "@tanstack/react-query";
import { AttioCore } from "../core.js";
import { entriesOverwrite } from "../funcs/entriesOverwrite.js";
import { combineSignals } from "../lib/primitives.js";
import { RequestOptions } from "../lib/sdks.js";
import * as operations from "../models/operations/index.js";
import { unwrapAsync } from "../types/fp.js";
import { useAttioContext } from "./_context.js";
import { MutationHookOptions } from "./_types.js";

export type EntriesOverwriteMutationVariables = {
  request: operations.PutV2ListsListEntriesEntryIdRequest;
  options?: RequestOptions;
};

export type EntriesOverwriteMutationData =
  operations.PutV2ListsListEntriesEntryIdResponseBody;

/**
 * Update a list entry (overwrite multiselect values)
 *
 * @remarks
 * Use this endpoint to update list entries by `entry_id`. If the update payload includes multiselect attributes, the values supplied will overwrite/remove the list of values that already exist (if any). Use the `PATCH` endpoint to add multiselect attribute values without removing those value that already exist.
 *
 * Required scopes: `list_entry:read-write`, `list_configuration:read`.
 */
export function useEntriesOverwriteMutation(
  options?: MutationHookOptions<
    EntriesOverwriteMutationData,
    Error,
    EntriesOverwriteMutationVariables
  >,
): UseMutationResult<
  EntriesOverwriteMutationData,
  Error,
  EntriesOverwriteMutationVariables
> {
  const client = useAttioContext();
  return useMutation({
    ...buildEntriesOverwriteMutation(client, options),
    ...options,
  });
}

export function mutationKeyEntriesOverwrite(): MutationKey {
  return ["attio-js", "Entries", "overwrite"];
}

export function buildEntriesOverwriteMutation(
  client$: AttioCore,
  hookOptions?: RequestOptions,
): {
  mutationKey: MutationKey;
  mutationFn: (
    variables: EntriesOverwriteMutationVariables,
  ) => Promise<EntriesOverwriteMutationData>;
} {
  return {
    mutationKey: mutationKeyEntriesOverwrite(),
    mutationFn: function entriesOverwriteMutationFn({
      request,
      options,
    }): Promise<EntriesOverwriteMutationData> {
      const mergedOptions = {
        ...hookOptions,
        ...options,
        fetchOptions: {
          ...hookOptions?.fetchOptions,
          ...options?.fetchOptions,
          signal: combineSignals(
            hookOptions?.fetchOptions?.signal,
            options?.fetchOptions?.signal,
          ),
        },
      };
      return unwrapAsync(entriesOverwrite(
        client$,
        request,
        mergedOptions,
      ));
    },
  };
}
