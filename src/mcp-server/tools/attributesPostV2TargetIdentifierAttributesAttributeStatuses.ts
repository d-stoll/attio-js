/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { attributesPostV2TargetIdentifierAttributesAttributeStatuses } from "../../funcs/attributesPostV2TargetIdentifierAttributesAttributeStatuses.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request:
    operations
      .PostV2TargetIdentifierAttributesAttributeStatusesRequest$inboundSchema,
};

export const tool$attributesPostV2TargetIdentifierAttributesAttributeStatuses:
  ToolDefinition<typeof args> = {
    name: "attributes-post-v2-target-identifier-attributes-attribute-statuses",
    description: `Create a status

Add a new status to a status attribute on either an object or a list.

Required scopes: \`object_configuration:read-write\`.`,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] =
        await attributesPostV2TargetIdentifierAttributesAttributeStatuses(
          client,
          args.request,
          { fetchOptions: { signal: ctx.signal } },
        ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      const value = result.value;

      return formatResult(value, apiCall);
    },
  };
