/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { objectsGetV2Objects } from "../../funcs/objectsGetV2Objects.js";
import { formatResult, ToolDefinition } from "../tools.js";

export const tool$objectsGetV2Objects: ToolDefinition = {
  name: "objects-get-v2-objects",
  description: `List objects

Lists all system-defined and user-defined objects in your workspace.

Required scopes: \`object_configuration:read\`.`,
  tool: async (client, ctx) => {
    const [result, apiCall] = await objectsGetV2Objects(
      client,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
