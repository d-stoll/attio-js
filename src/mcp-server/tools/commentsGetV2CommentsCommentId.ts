/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { commentsGetV2CommentsCommentId } from "../../funcs/commentsGetV2CommentsCommentId.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetV2CommentsCommentIdRequest$inboundSchema,
};

export const tool$commentsGetV2CommentsCommentId: ToolDefinition<typeof args> =
  {
    name: "comments-get-v2-comments-comment-id",
    description: `Get a comment

Get a single comment by ID.

To view comments on records, you will need the \`object_configuration:read\` and \`record_permission:read\` scopes.

To view comments on list entries, you will need the \`list_configuration:read\` and \`list_entry:read\` scopes.

Required scopes: \`comment:read\`.`,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] = await commentsGetV2CommentsCommentId(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      const value = result.value;

      return formatResult(value, apiCall);
    },
  };
