/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { metaGetV2Self } from "../../funcs/metaGetV2Self.js";
import { formatResult, ToolDefinition } from "../tools.js";

export const tool$metaGetV2Self: ToolDefinition = {
  name: "meta-get-v2-self",
  description: `Identify

Identify the current access token, the workspace it is linked to, and any permissions it has.`,
  tool: async (client, ctx) => {
    const [result, apiCall] = await metaGetV2Self(
      client,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
