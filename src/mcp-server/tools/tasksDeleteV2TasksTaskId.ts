/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { tasksDeleteV2TasksTaskId } from "../../funcs/tasksDeleteV2TasksTaskId.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.DeleteV2TasksTaskIdRequest$inboundSchema,
};

export const tool$tasksDeleteV2TasksTaskId: ToolDefinition<typeof args> = {
  name: "tasks-delete-v2-tasks-task-id",
  description: `Delete a task

Delete a task by ID.

Required scopes: \`task:read-write\`.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await tasksDeleteV2TasksTaskId(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
