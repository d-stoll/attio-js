/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { webhooksPatchV2WebhooksWebhookId } from "../../funcs/webhooksPatchV2WebhooksWebhookId.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.PatchV2WebhooksWebhookIdRequest$inboundSchema,
};

export const tool$webhooksPatchV2WebhooksWebhookId: ToolDefinition<
  typeof args
> = {
  name: "webhooks-patch-v2-webhooks-webhook-id",
  description: `Update a webhook

Update a webhook and associated subscriptions.

Required scopes: \`webhook:read-write\`.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await webhooksPatchV2WebhooksWebhookId(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
