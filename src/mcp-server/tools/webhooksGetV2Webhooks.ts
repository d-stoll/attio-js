/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { webhooksGetV2Webhooks } from "../../funcs/webhooksGetV2Webhooks.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetV2WebhooksRequest$inboundSchema,
};

export const tool$webhooksGetV2Webhooks: ToolDefinition<typeof args> = {
  name: "webhooks-get-v2-webhooks",
  description: `List webhooks

Get all of the webhooks in your workspace.

Required scopes: \`webhook:read\`.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await webhooksGetV2Webhooks(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
