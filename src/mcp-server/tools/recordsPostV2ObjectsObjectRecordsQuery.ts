/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { recordsPostV2ObjectsObjectRecordsQuery } from "../../funcs/recordsPostV2ObjectsObjectRecordsQuery.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.PostV2ObjectsObjectRecordsQueryRequest$inboundSchema,
};

export const tool$recordsPostV2ObjectsObjectRecordsQuery: ToolDefinition<
  typeof args
> = {
  name: "records-post-v2-objects-object-records-query",
  description: `List records

Lists people, company or other records, with the option to filter and sort results.

Required scopes: \`record_permission:read\`, \`object_configuration:read\`.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await recordsPostV2ObjectsObjectRecordsQuery(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
