/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { notesDeleteV2NotesNoteId } from "../../funcs/notesDeleteV2NotesNoteId.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.DeleteV2NotesNoteIdRequest$inboundSchema,
};

export const tool$notesDeleteV2NotesNoteId: ToolDefinition<typeof args> = {
  name: "notes-delete-v2-notes-note-id",
  description: `Delete a note

Delete a single note by ID.

Required scopes: \`note:read-write\`.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await notesDeleteV2NotesNoteId(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
